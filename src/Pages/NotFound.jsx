import Layout from "../components/Layout"

export default function NotFound () {
    return ( 
        <Layout>
            <h1 className="font-semibold text-subtitle">
                Not found page
            </h1>
            <p>
                We haven't found the page you selected
            </p>
        </Layout>
    )
}